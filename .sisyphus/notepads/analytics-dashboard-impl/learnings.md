Summary of changes on branch:
- Added a new analytics subsystem for blog:
  - Public analytics endpoint at blog/app/api/analytics/route.ts
  - Secure analytics ingestion for conversions at blog/app/api/analytics/conversions/route.ts
  - Core analytics logic and helpers at blog/lib/analytics.ts
  - Ingest helpers at blog/lib/analytics-ingest.ts
  - Client-side attribution and event tracking at blog/lib/analytics-client.ts and blog/components/tracked-external-link.tsx, blog/components/article-attribution.tsx
  - Unit tests for analytics at blog/tests/analytics*.test.ts
  - Views API under blog/app/api/views/[slug]/route.ts (updated to support view counting)
  - Dashboard surface in dashboard.py to surface analytics and git/R2 metrics
- Updated env template .env.example to reflect analytics ingest tokens and related vars
- Dashboard UI now contains wiring to surface analytics data (complex HTML/JS inline in dashboard.py)

Patterns and conventions observed:
- Strong separation of concerns: analytics.ts handles counters; ingest.ts handles tokens and deduping; API routes glue and validate; client.ts handles localStorage attribution; components wrap UI with tracking hooks.
- Use of Upstash Redis for metrics storage, with dedupe keys to prevent double-counting and a TTL for dedupe entries.
- Client-side tracking uses a dedicated analytics client that posts to /api/analytics with a merged payload including attribution if present.
- Tests cover name validation, key encoding, metrics mapping, and payload normalization.
- Env and config usage follows the repository-wide guardrails: tokens pulled from environment, no secrets in code.

Next steps for maintainers:
- Verify compatibility of new analytics routes with existing Upstash config in environment (.env and blog/.env.local).
- Review new tests to ensure they reflect production constraints and edge cases (e.g., missing tokens, invalid payloads).
- Ensure dashboard.py is secure and does not expose secrets; confirm that authentication/authorization is scoped correctly.

This note is auto-generated by the exploration tool. Please update as needed.
